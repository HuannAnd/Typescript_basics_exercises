export const Exercise1 = (): number[] => {
    // a)Os números de 1 a 10 de forma crescente
    
    let counter: number[]
    for (let index = 1; index < 11; index++) {
        counter.push(index)
    }

    return counter
}
